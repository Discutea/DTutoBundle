{% extends "DTutoBundle::layout.html.twig" %}

{% block title %}{{ 'discutea.tuto.category.form.title'|trans }}{% endblock %}


{% block body %}
    {% if form is defined %}
  <div class="col-md-12">
    <h1>{{ 'discutea.tuto.category.form.title'|trans }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
  </div>
    {% else %}
        {{ dump(tutorial) }}
        
    {% endif %}
{% endblock %}
