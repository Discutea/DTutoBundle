{% extends 'DTutoBundle::layout.html.twig' %}

{% block title %}{{ 'discutea.tuto.category.form.deletetitle'|trans }}{% endblock %}

{% block body %}
  <div class="col-md-12">
    <h1>{{ 'discutea.tuto.category.form.deletetitle'|trans }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">{{ 'discutea.tuto.btn.submit'|trans }}</button>
    {{ form_end(form) }}
  </div>
{% endblock %}
